import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Hoster Live - Validación",
};

export default function ValidateLayout({ children }: { children: React.ReactNode }) {
  return children;
}
